[build]
command = "python3 generate_config.py && cp -r . public 2>/dev/null || true"
publish = "."

[build.environment]
PYTHON_VERSION = "3.11"

# 重定向規則
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
